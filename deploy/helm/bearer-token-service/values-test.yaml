# ========================================
# Bearer Token Service V2 - 测试环境配置
# ========================================
# 使用内置 MongoDB + Redis

replicaCount: 1

# 启用内置 MongoDB
mongodb:
  enabled: true
  auth:
    username: admin
    password: changeme-testing
    database: token_service_v2

# 启用内置 Redis
redis:
  enabled: true

# 启用 Ingress
ingress:
  enabled: true
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
  host: bearer-token-test.jfcs-k8s-qa1.qiniu.io

# Qconf 配置（用于 /api/v2/validateu 获取用户信息）
qconf:
  enabled: true
  accessKey: "-lnWyW53aRF1AYUj5D2oBBub377cTMYawZdOT25z"
  secretKey: "FOXDmvIMttefUbUO8RWFIJ8JJdbPwvQGwoqZQL3O"
  masterHosts: "http://kodo-dev.confg.jfcs-k8s-qa2.qiniu.io"
  mcHosts: ""
  lcacheExpiresMs: "3000"
  lcacheDurationMs: "1000"
  lcacheChanBufSize: "16000"
  mcRWTimeoutMs: "100"

# 测试环境配置
config:
  ginMode: "debug"
  logLevel: "debug"
  skipIndexCreation: "false"
  rateLimit:
    app: false
    account: false
    token: false
